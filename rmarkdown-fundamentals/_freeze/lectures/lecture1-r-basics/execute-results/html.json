{
  "hash": "cbe149722b45db4a953b96a7e0b95e41",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"第1讲：R语言基础\"\nsubtitle: \"数据类型、结构与基础操作\"\n---\n\n## 本讲概述\n\n本讲将介绍R语言的核心基础，包括：\n\n- R语言的特点与应用领域\n- 五种基本数据类型\n- 四种核心数据结构\n- 向量化操作的优势\n- R包管理\n\n**预计时长**：45分钟理论 + 15分钟练习\n\n---\n\n## 1. R语言简介\n\n### 1.1 什么是R？\n\nR是由Ross Ihaka和Robert Gentleman在1995年开发的统计计算语言。它结合了：\n\n- **S语言**的语法特点\n- **Scheme语言**的底层实现\n- **开源社区**的持续贡献\n\n### 1.2 R的核心优势\n\n| 优势 | 说明 |\n|------|------|\n| 开源免费 | GPL许可，无使用限制 |\n| 统计专长 | 内置大量统计方法 |\n| 可视化强大 |  publication-quality图表 |\n| 扩展性强 | 20,000+ 扩展包 |\n| 社区活跃 | 全球统计学家支持 |\n\n### 1.3 生物医学应用\n\n- 临床试验数据分析\n- 基因组学/转录组学分析\n- 医学影像数据处理\n- 流行病学建模\n- 药物发现与开发\n\n---\n\n## 2. 基础数据类型\n\n### 2.1 类型系统\n\nR是**动态类型**语言，但每个对象都有明确的类型：\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 数值型 (numeric)\nx <- 3.14159\nclass(x)  # \"numeric\"\n\n# 整型 (integer)  \ny <- 42L\nclass(y)  # \"integer\"\n\n# 字符型 (character)\nname <- \"Central South University\"\nclass(name)  # \"character\"\n\n# 逻辑型 (logical)\nflag <- TRUE  # 或 FALSE, T, F\nclass(flag)   # \"logical\"\n\n# 复数型 (complex)\nz <- 3 + 4i\nclass(z)  # \"complex\"\n```\n:::\n\n\n### 2.2 类型转换\n\n\n::: {.cell}\n\n```{.r .cell-code}\nas.numeric(\"42\")      # 字符转数值\nas.character(42)      # 数值转字符\nas.logical(1)         # 1 → TRUE, 0 → FALSE\nas.integer(3.7)       # 截断取整\n```\n:::\n\n\n---\n\n## 3. 数据结构\n\n### 3.1 向量 (Vector)\n\n向量是R最基础的**一维**数据结构。\n\n**创建向量**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# c() 函数 (concatenate)\nnums <- c(1, 2, 3, 4, 5)\nchars <- c(\"a\", \"b\", \"c\")\nmixed <- c(1, \"a\")  # 自动转换为字符型\n\n# 序列生成\n1:10                           # 1到10\nseq(1, 10, by = 2)             # 步长为2\nseq(1, 10, length.out = 5)     # 均分5个点\nrep(1:3, each = 2)             # 每个重复2次\nrep(1:3, times = 2)            # 整体重复2次\n```\n:::\n\n\n**向量操作**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- c(10, 20, 30, 40, 50)\n\n# 索引 (R从1开始！)\nx[1]           # 第一个元素\nx[c(1, 3, 5)]  # 第1、3、5个元素\nx[2:4]         # 第2到4个元素\nx[x > 25]      # 条件筛选\nx[-1]          # 排除第一个\n\n# 向量化运算\nx + 10         # 每个元素加10\nx * 2          # 每个元素乘2\nx > 30         # 每个元素比较\n```\n:::\n\n\n### 3.2 矩阵 (Matrix)\n\n矩阵是**二维**数据结构，所有元素类型相同。\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 创建矩阵\nm <- matrix(1:12, nrow = 3, ncol = 4)\nm\n\n# 按行填充\nmatrix(1:12, nrow = 3, byrow = TRUE)\n\n# 索引\nm[1, 2]        # 第1行第2列\nm[1, ]         # 第1行\nm[, 2]         # 第2列\nm[1:2, 2:3]    # 子矩阵\n\n# 矩阵运算\nm + 10         # 标量加法\nm * 2          # 标量乘法\nt(m)           # 转置\nm %*% t(m)     # 矩阵乘法\n```\n:::\n\n\n### 3.3 数据框 (Data Frame)\n\n数据框是**最重要的**数据结构，类似Excel表格。\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 创建数据框\npatients <- data.frame(\n  id = 1:5,\n  name = c(\"张三\", \"李四\", \"王五\", \"赵六\", \"孙七\"),\n  age = c(25, 34, 45, 28, 52),\n  gender = c(\"M\", \"F\", \"M\", \"F\", \"M\"),\n  bp = c(120, 135, 142, 118, 155),\n  stringsAsFactors = FALSE  # 字符不转因子\n)\n\n# 查看\nhead(patients, 3)    # 前3行\ntail(patients)       # 后6行\nstr(patients)        # 结构\nsummary(patients)    # 摘要统计\ndim(patients)        # 维度\nncol(patients)       # 列数\nnrow(patients)       # 行数\n```\n:::\n\n\n**访问方式**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 列访问\npatients$age                 # 使用$\npatients[[\"age\"]]            # 使用双方括号\npatients[, \"age\"]            # 使用单括号（返回向量）\npatients[, \"age\", drop = FALSE]  # 保持数据框\n\n# 行访问\npatients[1, ]                # 第1行\npatients[1:3, ]              # 第1-3行\npatients[patients$age > 30, ]  # 条件筛选\n\n# 行列同时\npatients[1:3, c(\"name\", \"age\")]\n```\n:::\n\n\n### 3.4 列表 (List)\n\n列表是最**灵活**的数据结构，可以存储不同类型。\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 创建列表\nexperiment <- list(\n  id = \"EXP001\",\n  date = \"2025-02-27\",\n  samples = patients,\n  measurements = c(12.5, 13.2, 11.8),\n  passed_qc = TRUE\n)\n\n# 访问\nexperiment$id                # 使用$\nexperiment[[\"samples\"]]      # 双方括号\nexperiment[[3]]              # 按位置\nexperiment$samples$age       # 嵌套访问\n\n# 列表操作\nlength(experiment)           # 元素个数\nnames(experiment)            # 获取名称\nexperiment$new_item <- \"value\"  # 添加元素\n```\n:::\n\n\n---\n\n## 4. 向量化操作\n\n### 4.1 向量化 vs 循环\n\n**循环方式**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- 1:1000000\nresult <- numeric(length(x))\n\nfor (i in seq_along(x)) {\n  result[i] <- x[i] ^ 2\n}\n```\n:::\n\n\n**向量化方式**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nresult <- x ^ 2\n```\n:::\n\n\n向量化代码：\n- ✅ 更简洁\n- ✅ 更易读\n- ✅ **更快**（C级别优化）\n\n### 4.2 apply家族\n\n当必须使用循环时，优先使用apply：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm <- matrix(1:12, nrow = 3)\n\n# apply - 矩阵/数组\napply(m, 1, sum)      # 按行求和 (MARGIN=1)\napply(m, 2, mean)     # 按列求均值 (MARGIN=2)\n\n# lapply/sapply - 列表/向量\nl <- list(a = 1:3, b = 4:6)\nlapply(l, sum)        # 返回列表\nsapply(l, sum)        # 返回向量\n\n# tapply - 分组计算\ntapply(patients$age, patients$gender, mean)\n```\n:::\n\n\n---\n\n## 5. R包管理\n\n### 5.1 包安装\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 从CRAN安装\ninstall.packages(\"ggplot2\")\n\n# 从Bioconductor安装\nif (!require(\"BiocManager\"))\n    install.packages(\"BiocManager\")\nBiocManager::install(\"DESeq2\")\n\n# 从GitHub安装\ninstall.packages(\"remotes\")\nremotes::install_github(\"tidyverse/dplyr\")\n```\n:::\n\n\n### 5.2 包加载\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(ggplot2)      # 加载包，失败报错\nrequire(dplyr)        # 加载包，失败返回FALSE\n\n# 使用命名空间\nggplot2::ggplot(mtcars, aes(x = wt, y = mpg))  # 不加载直接使用\n```\n:::\n\n\n### 5.3 包管理\n\n\n::: {.cell}\n\n```{.r .cell-code}\ninstalled.packages()          # 查看已安装包\nupdate.packages()             # 更新所有包\nremove.packages(\"pkg_name\")   # 卸载包\n```\n:::\n\n\n---\n\n## 关键概念总结\n\n| 概念 | 要点 |\n|------|------|\n| 向量 | 一维，同类型，最基础 |\n| 矩阵 | 二维，同类型，数学运算 |\n| 数据框 | 二维，列可不同类型，最重要 |\n| 列表 | 递归，最灵活 |\n| 向量化 | 优先使用，效率更高 |\n| 包 | 扩展功能，CRAN/Bioconductor/GitHub |\n\n---\n\n## 课堂练习\n\n1. 创建一个包含以下信息的数据框：\n   - 5个学生，包含学号、姓名、成绩\n   - 筛选出成绩大于80分的学生\n   - 计算平均成绩\n\n2. 比较循环和向量化计算100万个数的平方：\n   - 使用 `system.time()` 测量时间\n\n3. 安装并加载 `tidyverse` 包\n\n---\n\n## 课后作业\n\n1. 阅读 [R for Data Science Chapter 1-3](https://r4ds.had.co.nz/)\n2. 完成实验手册练习1\n3. 熟悉RStudio界面和快捷键\n\n---\n\n**下节预告**：控制流与函数编写\n",
    "supporting": [
      "lecture1-r-basics_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}