---
title: "实验1：R基础编程"
subtitle: "掌握R语言核心语法与数据处理"
---

## 实验目标

完成本实验后，你将能够：

1. 熟练创建和操作R的各种数据结构
2. 使用控制流语句解决实际问题
3. 编写自定义函数封装重复代码
4. 进行基础的数据处理与分析

**实验时长**：60分钟  
**实验类型**：个人完成

---

## 实验准备

### 环境要求

- R >= 4.3.0
- RStudio >= 2023.06

### 创建新项目

1. 打开RStudio
2. 菜单：File → New Project → New Directory → New Project
3. 目录名：`lab1-r-basics`
4. 勾选 `Create a git repository`（如果有Git）

### 创建脚本文件

在RStudio中，创建新脚本：`File → New File → R Script`，保存为 `lab1.R`

---

## 第一部分：数据类型与结构 (20分钟)

### 练习1.1：基础类型

在R脚本中完成以下操作：

```r
# TODO: 创建以下变量
# 1. 一个数值型变量，值为 3.14159，命名为 pi_val
# 2. 一个整型变量，值为 100，命名为 n_samples  
# 3. 一个字符型变量，值为 "Biomedical Informatics"，命名为 dept_name
# 4. 一个逻辑型变量，值为 TRUE，命名为 is_student

# 你的代码：




# 使用 class() 函数检查每个变量的类型

```

::: {.hint-box}
**提示**：使用 `L` 后缀创建整数，如 `100L`
:::

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 创建变量
pi_val <- 3.14159
n_samples <- 100L
dept_name <- "Biomedical Informatics"
is_student <- TRUE

# 检查类型
cat("pi_val 类型:", class(pi_val), "\n")
cat("n_samples 类型:", class(n_samples), "\n")
cat("dept_name 类型:", class(dept_name), "\n")
cat("is_student 类型:", class(is_student), "\n")
```
</details>

---

### 练习1.2：向量操作

```r
# TODO: 完成以下向量操作

# 1. 创建一个包含 1 到 20 的向量，命名为 vec1
vec1 <- ____

# 2. 创建 seq() 生成 0 到 100、步长为 10 的向量
vec2 <- ____

# 3. 用 rep() 创建 "A", "B", "C" 各重复3次的向量
vec3 <- ____

# 4. 计算 vec1 的平均值、标准差、最大值
mean_val <- ____
sd_val <- ____
max_val <- ____

# 5. 提取 vec1 中大于10的元素
vec1_large <- ____

# 6. 将 vec1 的第 5 到第 10 个元素替换为 99
vec1[____] <- ____
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 1. 创建向量
vec1 <- 1:20
cat("vec1:", vec1, "\n\n")

# 2. seq生成序列
vec2 <- seq(0, 100, by = 10)
cat("vec2:", vec2, "\n\n")

# 3. rep重复
vec3 <- rep(c("A", "B", "C"), each = 3)
cat("vec3:", vec3, "\n\n")

# 4. 统计计算
mean_val <- mean(vec1)
sd_val <- sd(vec1)
max_val <- max(vec1)
cat("平均值:", mean_val, "标准差:", sd_val, "最大值:", max_val, "\n\n")

# 5. 条件筛选
vec1_large <- vec1[vec1 > 10]
cat("大于10的元素:", vec1_large, "\n\n")

# 6. 元素替换
vec1[5:10] <- 99
cat("替换后的vec1:", vec1, "\n")
```
</details>

---

### 练习1.3：数据框创建

创建一个患者信息数据框：

```r
# TODO: 创建如下数据框
# 患者ID: 101, 102, 103, 104, 105
# 姓名: 张三, 李四, 王五, 赵六, 孙七
# 年龄: 25, 34, 45, 28, 52
# 性别: 男, 女, 男, 女, 男
# 收缩压: 120, 135, 142, 118, 155

patients <- data.frame(
  # 你的代码：
  
  
  
  
  
)

# 查看数据框结构
str(patients)

# 查看摘要统计
summary(patients)
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 创建数据框
patients <- data.frame(
  id = 101:105,
  name = c("张三", "李四", "王五", "赵六", "孙七"),
  age = c(25, 34, 45, 28, 52),
  gender = c("男", "女", "男", "女", "男"),
  bp = c(120, 135, 142, 118, 155),
  stringsAsFactors = FALSE
)

# 查看结构
cat("数据框结构:\n")
str(patients)

cat("\n摘要统计:\n")
summary(patients)

cat("\n数据框内容:\n")
print(patients)
```
</details>

---

### 练习1.4：数据框操作

```r
# TODO: 基于上题的 patients 数据框

# 1. 提取所有女性患者的信息
female_patients <- ____

# 2. 提取年龄大于40岁的患者姓名
older_names <- ____

# 3. 计算所有患者的平均年龄
avg_age <- ____

# 4. 找出收缩压最高的患者ID
high_bp_id <- ____

# 5. 添加一列 "风险等级"：收缩压>=140为"高"，否则为"正常"
patients$risk <- ifelse(____, ____, ____)
```

::: {.hint-box}
**提示**：
- 条件筛选使用 `data[condition, ]`
- `which.max()` 可以找到最大值的位置
:::

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 重新创建数据框（确保存在）
patients <- data.frame(
  id = 101:105,
  name = c("张三", "李四", "王五", "赵六", "孙七"),
  age = c(25, 34, 45, 28, 52),
  gender = c("男", "女", "男", "女", "男"),
  bp = c(120, 135, 142, 118, 155),
  stringsAsFactors = FALSE
)

# 1. 提取女性患者
female_patients <- patients[patients$gender == "女", ]
cat("女性患者:\n")
print(female_patients)

# 2. 提取年龄大于40岁的患者姓名
older_names <- patients$name[patients$age > 40]
cat("\n年龄大于40岁的患者:", older_names, "\n")

# 3. 计算平均年龄
avg_age <- mean(patients$age)
cat("平均年龄:", avg_age, "\n")

# 4. 找出收缩压最高的患者ID
high_bp_id <- patients$id[which.max(patients$bp)]
cat("收缩压最高的患者ID:", high_bp_id, "\n")

# 5. 添加风险等级列
patients$risk <- ifelse(patients$bp >= 140, "高", "正常")
cat("\n添加风险等级后的数据框:\n")
print(patients)
```
</details>

---

## 第二部分：控制流 (15分钟)

### 练习2.1：BMI计算与分类

完成BMI计算函数：

```r
# TODO: 完善以下函数
calculate_bmi <- function(weight_kg, height_m) {
  # 1. 计算BMI
  bmi <- ____
  
  # 2. 根据BMI判断分类
  # < 18.5: 偏瘦
  # 18.5 - 23.9: 正常
  # 24 - 27.9: 超重
  # >= 28: 肥胖
  if (____) {
    category <- "偏瘦"
  } else if (____) {
    category <- "正常"
  } else if (____) {
    category <- "超重"
  } else {
    category <- "肥胖"
  }
  
  # 3. 返回列表
  return(list(
    bmi = round(bmi, 2),
    category = category
  ))
}

# 测试函数
calculate_bmi(70, 1.75)
calculate_bmi(55, 1.60)
calculate_bmi(85, 1.70)
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# BMI计算函数
calculate_bmi <- function(weight_kg, height_m) {
  # 1. 计算BMI
  bmi <- weight_kg / (height_m ^ 2)
  
  # 2. 根据BMI判断分类
  if (bmi < 18.5) {
    category <- "偏瘦"
  } else if (bmi < 24) {
    category <- "正常"
  } else if (bmi < 28) {
    category <- "超重"
  } else {
    category <- "肥胖"
  }
  
  # 3. 返回列表
  return(list(
    bmi = round(bmi, 2),
    category = category
  ))
}

# 测试函数
cat("测试1 (70kg, 1.75m):\n")
result1 <- calculate_bmi(70, 1.75)
print(result1)

cat("\n测试2 (55kg, 1.60m):\n")
result2 <- calculate_bmi(55, 1.60)
print(result2)

cat("\n测试3 (85kg, 1.70m):\n")
result3 <- calculate_bmi(85, 1.70)
print(result3)
```
</details>

---

### 练习2.2：循环应用

```r
# TODO: 使用循环完成以下任务

# 1. 用 for 循环计算 1 到 100 的平方和
sum_squares <- 0
for (____) {
  ____
}
sum_squares

# 验证：使用向量化方法
sum((1:100)^2)

# 2. 用 while 循环找出第一个大于 1000 的斐波那契数
fib_a <- 1
fib_b <- 1
while (____) {
  temp <- fib_a + fib_b
  fib_a <- fib_b
  fib_b <- temp
}
fib_b  # 第一个大于1000的斐波那契数

# 3. 用循环计算 patients 数据框中每个患者的BMI
# 假设体重数据如下(kg)：
patients$weight <- c(65, 58, 72, 55, 80)
patients$height <- c(1.70, 1.60, 1.75, 1.58, 1.72)

# 初始化BMI列
patients$bmi <- NA

for (____) {
  patients$bmi[i] <- ____
}

patients
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 1. for循环计算平方和
sum_squares <- 0
for (i in 1:100) {
  sum_squares <- sum_squares + i^2
}
cat("1到100的平方和(循环):", sum_squares, "\n")
cat("1到100的平方和(向量化):", sum((1:100)^2), "\n\n")

# 2. while循环找斐波那契数
fib_a <- 1
fib_b <- 1
while (fib_b <= 1000) {
  temp <- fib_a + fib_b
  fib_a <- fib_b
  fib_b <- temp
}
cat("第一个大于1000的斐波那契数:", fib_b, "\n\n")

# 3. 循环计算BMI
patients <- data.frame(
  id = 101:105,
  name = c("张三", "李四", "王五", "赵六", "孙七"),
  weight = c(65, 58, 72, 55, 80),
  height = c(1.70, 1.60, 1.75, 1.58, 1.72),
  stringsAsFactors = FALSE
)
patients$bmi <- NA

for (i in 1:nrow(patients)) {
  patients$bmi[i] <- round(patients$weight[i] / (patients$height[i] ^ 2), 2)
}

cat("患者BMI计算结果:\n")
print(patients)
```
</details>

---

## 第三部分：综合应用 (20分钟)

### 练习3.1：基因表达分析

假设你有一个基因表达矩阵：

```{r}
#| echo: true
#| eval: true

# 基因表达数据（行：基因，列：样本）
gene_exp <- data.frame(
  gene_id = c("TP53", "BRCA1", "EGFR", "MYC", "KRAS"),
  sample1 = c(12.5, 8.3, 15.2, 20.1, 6.7),
  sample2 = c(11.8, 9.1, 14.5, 19.3, 7.2),
  sample3 = c(13.2, 7.9, 16.1, 21.5, 6.1),
  sample4 = c(10.5, 8.7, 13.8, 18.2, 7.8)
)

print(gene_exp)
```

**任务**：

```r
# TODO: 完成以下分析任务

# 1. 创建一个函数，计算每行（基因）的：
#    - 平均表达量
#    - 表达量标准差
#    - 变异系数 (CV = SD / Mean)
analyze_gene <- function(exp_values) {
  # 你的代码：
  
}

# 2. 对所有基因应用该函数，结果保存为新数据框
gene_summary <- data.frame(
  gene_id = gene_exp$gene_id,
  mean_exp = NA,
  sd_exp = NA,
  cv = NA
)

for (i in 1:nrow(gene_exp)) {
  result <- analyze_gene(____)
  gene_summary$mean_exp[i] <- ____
  gene_summary$sd_exp[i] <- ____
  gene_summary$cv[i] <- ____
}

gene_summary

# 3. 找出平均表达量最高的基因
most_expressed <- ____

# 4. 找出变异系数最大的基因（表达最不稳定）
most_variable <- ____
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 重新创建数据
gene_exp <- data.frame(
  gene_id = c("TP53", "BRCA1", "EGFR", "MYC", "KRAS"),
  sample1 = c(12.5, 8.3, 15.2, 20.1, 6.7),
  sample2 = c(11.8, 9.1, 14.5, 19.3, 7.2),
  sample3 = c(13.2, 7.9, 16.1, 21.5, 6.1),
  sample4 = c(10.5, 8.7, 13.8, 18.2, 7.8)
)

# 1. 创建分析函数
analyze_gene <- function(exp_values) {
  mean_val <- mean(exp_values)
  sd_val <- sd(exp_values)
  cv_val <- sd_val / mean_val
  
  return(list(
    mean = mean_val,
    sd = sd_val,
    cv = cv_val
  ))
}

# 2. 应用到所有基因
gene_summary <- data.frame(
  gene_id = gene_exp$gene_id,
  mean_exp = NA,
  sd_exp = NA,
  cv = NA
)

for (i in 1:nrow(gene_exp)) {
  # 提取表达值（排除gene_id列）
  exp_values <- as.numeric(gene_exp[i, -1])
  result <- analyze_gene(exp_values)
  
  gene_summary$mean_exp[i] <- round(result$mean, 2)
  gene_summary$sd_exp[i] <- round(result$sd, 2)
  gene_summary$cv[i] <- round(result$cv, 4)
}

cat("基因表达分析结果:\n")
print(gene_summary)

# 3. 平均表达量最高的基因
most_expressed <- gene_summary$gene_id[which.max(gene_summary$mean_exp)]
cat("\n平均表达量最高的基因:", most_expressed, "\n")

# 4. 变异系数最大的基因
most_variable <- gene_summary$gene_id[which.max(gene_summary$cv)]
cat("变异系数最大的基因:", most_variable, "\n")
```
</details>

---

### 练习3.2：质控过滤

```r
# TODO: 基于 gene_summary 进行过滤

# 1. 过滤条件：
#    - 平均表达量 >= 10
#    - 变异系数 <= 0.15
# 保留满足条件的基因

filtered_genes <- gene_summary[____, ]
filtered_genes

# 2. 统计满足条件的基因数量
cat("原始基因数:", nrow(gene_summary), "\n")
cat("过滤后基因数:", ____, "\n")
cat("过滤比例:", ____, "%\n")
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 重新创建 gene_summary
gene_exp <- data.frame(
  gene_id = c("TP53", "BRCA1", "EGFR", "MYC", "KRAS"),
  sample1 = c(12.5, 8.3, 15.2, 20.1, 6.7),
  sample2 = c(11.8, 9.1, 14.5, 19.3, 7.2),
  sample3 = c(13.2, 7.9, 16.1, 21.5, 6.1),
  sample4 = c(10.5, 8.7, 13.8, 18.2, 7.8)
)

gene_summary <- data.frame(
  gene_id = gene_exp$gene_id,
  mean_exp = c(12.0, 8.5, 14.9, 19.78, 6.95),
  sd_exp = c(1.14, 0.69, 1.12, 1.37, 0.72),
  cv = c(0.095, 0.081, 0.075, 0.069, 0.104)
)

# 1. 质控过滤
filtered_genes <- gene_summary[gene_summary$mean_exp >= 10 & gene_summary$cv <= 0.15, ]
cat("通过质控的基因:\n")
print(filtered_genes)

# 2. 统计信息
original_count <- nrow(gene_summary)
filtered_count <- nrow(filtered_genes)
filter_ratio <- (1 - filtered_count / original_count) * 100

cat("\n原始基因数:", original_count, "\n")
cat("过滤后基因数:", filtered_count, "\n")
cat("过滤比例:", round(filter_ratio, 1), "%\n")
```
</details>

---

### 练习3.3：临床数据整合

```r
# 新的临床数据
clinical <- data.frame(
  patient_id = c(101, 102, 103, 104, 105),
  diagnosis = c("Control", "Case", "Case", "Control", "Case"),
  age = c(25, 34, 45, 28, 52),
  treatment = c("Placebo", "Drug", "Drug", "Placebo", "Drug")
)

# 将 patients 与 clinical 合并
# 注意：patients 中的ID列可能需要调整

# TODO:
# 1. 重命名 patients 的ID列以便匹配
names(patients)[____] <- "patient_id"

# 2. 使用 merge() 合并两个数据框
merged_data <- merge(____, ____, by = ____)

# 3. 按 diagnosis 分组，计算平均年龄
# 提示：使用 aggregate() 或手动计算
control_mean_age <- mean(____)
case_mean_age <- mean(____)

cat("Control组平均年龄:", control_mean_age, "\n")
cat("Case组平均年龄:", case_mean_age, "\n")

# 4. 筛选出Case组且年龄>40的患者
case_older <- ____
case_older
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 重新创建数据
patients <- data.frame(
  id = 101:105,
  name = c("张三", "李四", "王五", "赵六", "孙七"),
  age = c(25, 34, 45, 28, 52),
  gender = c("男", "女", "男", "女", "男"),
  bp = c(120, 135, 142, 118, 155),
  stringsAsFactors = FALSE
)

clinical <- data.frame(
  patient_id = c(101, 102, 103, 104, 105),
  diagnosis = c("Control", "Case", "Case", "Control", "Case"),
  age = c(25, 34, 45, 28, 52),
  treatment = c("Placebo", "Drug", "Drug", "Placebo", "Drug")
)

# 1. 重命名ID列
names(patients)[names(patients) == "id"] <- "patient_id"

# 2. 合并数据框
merged_data <- merge(patients, clinical, by = "patient_id")
cat("合并后的数据:\n")
print(merged_data)

# 3. 分组计算平均年龄
control_mean_age <- mean(merged_data$age.y[merged_data$diagnosis == "Control"])
case_mean_age <- mean(merged_data$age.y[merged_data$diagnosis == "Case"])

cat("\nControl组平均年龄:", control_mean_age, "\n")
cat("Case组平均年龄:", case_mean_age, "\n")

# 4. 筛选Case组且年龄>40的患者
case_older <- merged_data[merged_data$diagnosis == "Case" & merged_data$age.y > 40, ]
cat("\nCase组且年龄>40的患者:\n")
print(case_older)
```
</details>

---

## 第四部分：拓展挑战 (5分钟)

### 挑战题：批量处理

```r
# 批量读取多个样本的数据文件
# 假设你有文件: sample1.txt, sample2.txt, sample3.txt

# TODO: 编写代码批量读取并合并

# 1. 创建一个函数，读取单个样本文件
# 格式：两列，gene 和 expression
read_sample <- function(filename, sample_name) {
  # 读取数据
  data <- read.table(filename, header = TRUE, sep = "\t")
  # 重命名表达列为样本名
  names(data)[2] <- sample_name
  return(data)
}

# 2. 使用循环批量读取多个样本并合并
# （模拟数据）
sample_files <- c("sample1", "sample2", "sample3")
combined_data <- NULL

for (file in sample_files) {
  # 模拟读取
  temp_data <- data.frame(
    gene = c("TP53", "BRCA1", "EGFR"),
    expression = runif(3, 5, 20)
  )
  names(temp_data)[2] <- file
  
  if (is.null(combined_data)) {
    combined_data <- temp_data
  } else {
    combined_data <- merge(combined_data, temp_data, by = "gene")
  }
}

combined_data
```

<details>
<summary>点击查看参考答案</summary>

```{r}
#| eval: true
#| echo: true

# 模拟批量读取函数
read_sample <- function(filename, sample_name) {
  # 模拟读取数据
  data <- data.frame(
    gene = c("TP53", "BRCA1", "EGFR", "MYC", "KRAS"),
    expression = round(runif(5, 5, 20), 2)
  )
  names(data)[2] <- sample_name
  return(data)
}

# 批量读取并合并
sample_files <- c("sample1", "sample2", "sample3")
combined_data <- NULL

for (file in sample_files) {
  temp_data <- read_sample(file, file)
  
  if (is.null(combined_data)) {
    combined_data <- temp_data
  } else {
    combined_data <- merge(combined_data, temp_data, by = "gene")
  }
}

cat("合并后的表达矩阵:\n")
print(combined_data)
```
</details>

---

## 实验提交

### 提交内容

1. **R脚本文件** (`lab1.R`)
   - 包含所有练习的代码
   - 添加必要注释

2. **实验报告** (PDF或HTML)
   - 使用R Markdown编写
   - 包含关键代码和结果截图
   - 回答思考题

---

## 思考题

1. **向量化 vs 循环**
   - 为什么R中向量化操作通常比循环更快？
   - 在什么情况下必须使用循环？

2. **数据类型选择**
   - data.frame 和 matrix 的主要区别是什么？
   - 什么时候应该使用 list？

3. **函数设计**
   - 如何设计一个"好"的函数？
   - 参数命名有什么最佳实践？

---

## 延伸阅读

- [R for Data Science - Chapter 5](https://r4ds.had.co.nz/transform.html)
- [Advanced R - Data Structures](https://adv-r.hadley.nz/vectors-chap.html)
- [R Programming for Data Science](https://bookdown.org/rdpeng/rprogdatascience/)

---

**实验结束，祝学习愉快！** 🎉
